const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..'),worker=fs.readFileSync(path.join(root,'workerbackup.js'),'utf8'),auth=fs.readFileSync(path.join(root,'js','auth.js'),'utf8'),core=fs.readFileSync(path.join(root,'js','core.js'),'utf8'),index=fs.readFileSync(path.join(root,'index.html'),'utf8');let n=0;const ok=(v,m)=>{console.log(`${v?'OK':'FAIL'} ${m}`);if(!v)n++};ok(/dailyPointCheckin:\s*\{[^}]*allowD1Fallback:\s*true/.test(worker),'daily checkin has explicit fallback policy');ok(/extractLineVoomMedia:\s*\{[^}]*allowD1Fallback:\s*true/.test(worker),'media extraction has explicit fallback policy');ok(!(/async getActorFromD1Identity[\s\S]*?\n  \},/.exec(worker)||[''])[0].includes('pointUserId'),'payload pointUserId is not an actor fallback');ok(!/payload\.pt_uid[\s\S]{0,200}findUserByIdentity/.test(worker),'payload pt_uid is not an actor fallback');ok(/ACTMASTER_POINT_UID_/.test(auth),'point bridge cache remains');ok(/resolvePointUserId/.test(worker),'point identity resolver remains');ok(/LINE 授權更新中/.test(core),'token refresh feedback remains');ok(/js\/core\.js\?v=/.test(index)&&/js\/auth\.js\?v=/.test(index),'cache-bust references use version format');if(n)process.exit(1);console.log('Auth stability contract passed.');