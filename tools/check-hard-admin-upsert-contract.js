const fs=require('fs'),path=require('path');const w=fs.readFileSync(path.join(__dirname,'..','workerbackup.js'),'utf8');const ok=(v,m)=>{console.log(`${v?'OK':'FAIL'} ${m}`);if(!v)process.exitCode=1};ok(/const AdminRoleModule/.test(w),'D1 platform role module exists');ok(!/hardAdminAccounts|hardAdminIds|isHardAdmin|hasHardAdminId/.test(w),'legacy hard admin checks removed');ok(/platform_admin_role_audit/.test(w),'role changes are auditable');ok(/platform_admin_bootstrap_state/.test(w),'bootstrap is one-time');console.log('Platform admin role contract complete.');