const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const worker=fs.readFileSync(path.join(root,'workerbackup.js'),'utf8');let n=0;const ok=(v,m)=>{console.log(`${v?'OK':'FAIL'} ${m}`);if(!v)n++};ok(!/hardAdminAccounts|hardAdminIds|isHardAdmin|hasHardAdminId/.test(worker),'hard admin identifiers removed');ok(/const AdminRoleModule/.test(worker)&&/platform_admin_roles/.test(worker),'D1 platform role module exists');ok(/ADMIN_BOOTSTRAP_SECRET/.test(worker)&&/platform_admin_bootstrap_state/.test(worker),'bootstrap is secret-gated and one-time');ok(/Cannot revoke the last active platform admin/.test(worker),'last active admin protected');ok(/platform_admin_role_audit/.test(worker),'audit log exists');ok(/grantPlatformAdminRole: \{ access: 'admin' \}/.test(worker)&&/revokePlatformAdminRole: \{ access: 'admin' \}/.test(worker),'grant revoke admin-only');ok(/bootstrapPlatformAdmin: \{ access: 'authenticated'/.test(worker),'bootstrap authenticated');ok(!/hardAdminAccounts|hardAdminIds/.test(worker),'no legacy phone/name admin source remains');for(const f of ['tools/check-security-phase-2a-contract.js','tools/check-security-phase-2b-contract.js','tools/check-security-phase-2c-contract.js'])ok(fs.existsSync(path.join(root,f)),`${f} retained`);if(n)process.exit(1);console.log('Phase 2D security contract passed.');