# 後台登入等待與手機排版修正

- 需求來源：使用者截圖及「開始修正」，已授權直接提交、部署。
- 起始／回復點：98620e38496db4a92c080d8e18cd610994e98ad2。
- 修改範圍：admin.html、對應測試及本契約；只調整等待狀態、逾時重試、取消載入時補註冊、響應式排版與標題圖示文字。
- 禁止：Worker、點數、會員歸屬、角色判斷／權限提升、LINE secrets / LIFF ID / 轉址；不操作正式交易或補註冊。
- 已讀：核心不變規則、LIFF 路由、回歸矩陣、變更流程；新增 admin-entry 契約。
- guard before / after：PASS；6 項針對性測試全部通過；diff check PASS。
- 驗證：合成登入逾時／權限拒絕／成功、CRM 只讀載入、手機／平板版面；不登入正式後台做寫入測試。
- 發布：僅 GitHub Pages，無 Worker 部署、無 migration。
- 網頁效能技能：採用隔離只讀畫面核對 CSS 版面，保留驗證邊界；未用正式登入帳號觸發補註冊。
- 瀏覽器：390px 與 820px 內容寬度不溢出，預設側欄 64px，展開 256px 不擠動主內容；820px 說明文字區約 646px（修正前約 114px）。隔離畫面使用等尺寸圖示占位，未當作正式圖示或 LINE 登入耗時測試。
- 合成測試包含 SDK 載入／LINE 初始化／profile／權限等待逾時、遲到回應不可放行、拒絕一般／贈點／扣點角色、既有 store/admin 成功登入與外部導向不變、CRM 僅讀取不補註冊。
