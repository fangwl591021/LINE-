# 電話純扣點

- 需求：電話扣點呈現同電話贈點；使用者明確確認不涉及消費，授權提交、部署。
- 起始／回復點：c7eb92deb221294c19456aeca617883a9266d73a。
- 只改：電話扣點 POP、明確純扣點後端參數與紀錄標示、快取版本及相關測試。
- 不改：商品消費折抵、會員歸屬、贈點權限、秘密／綁定／資料庫結構；不操作正式點數。
- 已讀核心不變規則、點數帳本與收銀保護契約；guard before PASS。
- 使用現有餘額查詢、requestId、防重送和 unknown 查單邊界；角色在請求與寫入前確認。
- 驗證：109 項相關測試通過（含電話純扣點、權限、餘額不足、防重、紀錄及舊贈點／消費折抵）；guard before/after、diff check、Worker dry-run PASS。
- 本機瀏覽器：扣點用戶 → 電話查找 → 0912-345-678 合成會員 → 輸入 10 → 成功扣除 10 點；無消費欄位。僅模擬送出，未異動正式會員點數。
- Workers 安全技能用於保持後端權限重查、防重及失敗鎖定；Wrangler 技能用於 dry-run 與保留正式環境變數的部署。
- 發布：Worker --keep-vars，main 推送 Pages，核对正式資源。
