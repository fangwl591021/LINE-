# 生活好店會員點數操作入口

- 基準：main / b85308f。使用者先授權實作，後明確授權「提交、部署」。
- 本次只允許改什麼：品牌按鈕、本站 LIFF 導航、兩種會員辨識選項 POP、重用收銀機 DOM、關閉相機的非同步清理、版本與回歸測試。
- 本次禁止碰什麼：後端贈扣點政策、角色規則、共享點數帳戶、訂單收款設定、VEO API、真實點數交易。
- 參考 VEO 的互動，不複製其錢包；掃碼／手機沿用本站查找與確認送出、防重送、待確認交易查詢。
- 開啟選擇視窗不讀取點數、會員或流水帳；LIFF 導航僅帶 shopSection=cashier，不帶客戶或交易資料。
- guard:before / guard:after 全部通過；11 項專用 Node 測試與既有註冊 POP 5 項測試通過；git diff --check 通過。
- 補上相簿解碼的身分／世代失效檢查，以及同一商城容器重新 mount 時關閉舊 POP，避免舊 QR 回填或遮住商品入口。
- 320px / 390px 合成資料瀏覽器互動通過：兩選項、原輸入框焦點、明確查找姓名與點數、送出中禁止關閉／返回、掃碼器 Escape、節點復原、帳號切換／舊查詢失效、一般會員禁止進入。
- 合成測試結果：開啟流程的商城／錢包讀取 0、點數寫入 0；一筆明確觸發的假客戶查詢。截圖工具逾時，改驗 DOM 互動與尺寸；兩寬度 POP 不橫向溢出且關閉按鈕可見。
- 相機硬體／LINE 真機、LIFF 實際 OAuth 回跳及正式交易未實測；本機相機測試只驗證延遲取消，不以此聲稱真機成功。

## 續查

- 修正快速 cashier→cashier 重掛時舊 POP 搶走新入口：同步清理已失效的實例，再開啟新 POP。
- 新增 cashier 入口遇 NFC／活動核銷參數時讓出導航；不變更原有商城分支與簽到／點數規則。
- 新增 8 項 LIFF 參數、清理 OAuth 回跳 URL、會員身分順序與衝突路由的執行式 VM 測試，納入完整 guard。
- 原收銀機＋原 safe-cashier 以假回應完成贈／扣點、雙擊防重、失敗保留表單、逾時查原交易測試；4 次模擬送出，真實網路呼叫 0。
- 續查階段未提交／部署；LINE 真機、正式 OAuth 授權與真實點數交易仍未驗證。

## 發布範圍與預檢

- 按使用者指示合併本次與續查成果提交 main，由既有 GitHub Pages 流程發布。
- fetch 後 HEAD 與 origin/main 均為 b85308f，沒有遠端差異或其他工作樹。
- 完整 guard:after 通過；商城、收銀防重、錢包摘要、歷史與註冊 POP 的額外 50 項測試通過；diff --check 通過。
- 僅發布前端與相關測試／文件；Worker、D1 migration、正式網購開關均不變。不以部署成功取代 LINE 真機與真實交易驗證。
