# 後台登入與手機顯示

- 2026-09-20 使用者授權修正等待與手機版面；測試後提交、部署。
- LINE 初始化、個人資料及管理權限各顯示等待階段並有逾時；失敗或超時不得解除驗證遮罩，不得以快取或前端資料略過角色檢查。
- 重試採整頁重新載入，避免舊的非同步驗證結果與新請求交錯。
- CRM 首次讀取與重新整理不得自動補註冊；保留既有「同步已綁名片」按鈕及確認流程，不修改身份／歸屬寫入規則。
- 窄螢幕初始收合側欄，可手動展開；標題、操作鈕可換行，主內容不得被擠成直排。
- 既有 LIFF ID、登入轉址、允許後台角色、Worker、點數與秘密皆不改。
- 全站名片庫篩選列與操作鈕必須在容器內換行；長歸屬選項不得撐開頁面，寬表格保留原有內部橫向捲動，不更改查詢或同步行為。
